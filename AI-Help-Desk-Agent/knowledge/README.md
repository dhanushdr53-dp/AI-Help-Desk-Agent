# Knowledge base

Add trusted TXT/Markdown support articles here, then ingest them through the
`POST /api/knowledge` endpoint. Keep documents free from secrets and treat all
uploaded content as untrusted data.